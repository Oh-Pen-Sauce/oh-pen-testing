## Playbook: unrestricted-upload (scan)

Confirm when uploaded files are persisted without validating extension + MIME type + size. Do NOT confirm when uploads go to a known-safe quarantine store or are served from a different domain.
